# AI 协作会话总结

## 任务背景

面试题要求在 3 天内完成一个面向普通用户的视频格式转换器：加载视频信息并显示缩略图，通过 FFmpeg 命令行转换，做出合理优化，保留 AI 协作总结、开发者决策和反馈，最终以 GitHub 项目提交。

本次工作基于原模板项目继续开发，没有移除原有界面、批量转换、预览和桌面安全设置。

## 本次完成内容

1. 阅读项目结构和现有媒体转换实现，确认原项目已经使用 FFmpeg 命令行，并支持 H.264、H.265、AV1、VP9、GIF 和音频提取。
2. 新增视频信息探测：时长、分辨率、帧率、总码率、容器和音视频编码。
3. 新增视频缩略图：从视频约 10% 位置截帧，返回本地 JPEG 数据并展示在原界面中。
4. 新增“智能转换”：兼容时使用 FFmpeg `-codec copy` 无损直拷，不兼容时回退 H.264；用户仍可手动选择 H.264、H.265 或 AV1。
5. 转换结果新增实际执行方式、耗时和输入输出编码，界面会明确提示“无损直拷”或“已转码为某编码”。
6. 增加解析、策略与静态安全测试，并用固定 FFmpeg 真实验证接口和网页。
7. 增加可重复的性能/质量测试脚本和技术说明文档。
8. 按 Apple 设计系统重做桌面界面：使用克制的白、浅灰、近黑和单一蓝色操作色，重排顶部工具栏、四步流程、上传产品区、转换设置与格式能力卡片；（后续简化迭代中，状态动画与赞助入口被移除，详见 UI_SHIPPED.md。）
9. 将所选设计规范原样保存为项目根目录 `DESIGN.md`，并完成 1440px 桌面与 390px 窄屏的真实交互、截图和无溢出验证。

## 开发者决策与反馈记录

| 阶段 | 决策或反馈 | 落地结果 |
|---|---|---|
| 项目选择 | 用户指定基于原模板项目实现视频转换 | 克隆到独立目录，保留原项目结构 |
| 联网方式 | 用户要求以后查资料优先使用内置浏览器 | GitHub 项目与固定引擎发布页优先通过内置浏览器查看 |
| FFmpeg 接入 | 面试题要求命令行，不接 FFmpeg API | 所有探测、截帧和转换均通过子进程参数数组调用 FFmpeg |
| 优化目标 | 用户希望说明速度或质量提升及具体数字 | 选择可直接测量的智能直拷，与 H.264 CRF 23 重编码对比 |
| 数据可信度 | 不提前编造百分比 | 恢复项目固定 FFmpeg，生成固定样本，多次运行取中位数，并记录 SSIM/PSNR |
| 用户体验 | 项目目标是给人使用，界面选择很重要 | 在原上传区增加缩略图和六项信息，在转换结果中解释实际执行方式 |
| 兼容性 | 不能为了速度生成播放器打不开的文件 | 仅在容器与音视频编码兼容时直拷，否则自动转码 |
| 透明视频 | 原项目已有透明通道转背景色逻辑 | 透明视频禁止直拷，继续走背景合成和重新编码 |
| UI 设计系统 | 在内置设计库中比较 Apple、Notion 和 Linear；本项目是给普通用户使用的本地桌面工具，需要安静、可信且突出单一任务 | 采用 Apple 规范；Notion 的多色营销感和 Linear 的深色开发工具感都不如本方案匹配 |
| 品牌保留 | 重设计不能把原项目改成无辨识度的通用模板 | 沿用既有界面结构；品牌视觉在后续简化迭代中移除 |
| 响应式验收 | 桌面 UI 也需要兼容小窗口和长文案 | 真实验证 1440×1000 与 390×844，均无横向溢出、无浏览器错误 |

## 遇到的问题与处理

- 源码仓库不包含大型转换引擎。按仓库的固定清单下载 Windows 核心引擎包，核对 SHA-256 后只恢复 FFmpeg 8.1.1。
- 完整测试包含 Office、PDF 和 OCR 场景，缺少对应引擎时会失败。改为同时报告不依赖引擎的测试和视频专项真实测试，不把无关失败描述为本次回归。
- 初次质量对比受 MKV 首帧时间戳变化影响，出现错误的 SSIM。改为按帧序号重建时间轴后再比较，直拷结果为 SSIM 1.0、PSNR 无限。
- 本地页面禁止字符串形式的动态脚本执行，第一次浏览器测试被安全策略拦截。测试改为标准元素断言后通过，未降低页面安全策略。
- 原静态测试把粉红色主题色写死为通过条件，与本次明确授权的 UI 重设计冲突。测试改为验证新设计令牌，同时继续验证流程组件存在。

## 当前结论

本次改动的核心提升不是“所有视频都更小”，而是：当用户只是更换兼容容器时，不再无意义地重新压缩。在固定测试样本上，智能直拷比原 H.264 重编码快 20.65 倍、耗时减少 95.16%，画面保持逐帧一致；代价是没有通过降低码率来缩小文件。

## 后续可改进

- 解析 FFmpeg `-progress` 输出，展示真实逐帧进度并支持取消；
- 在探测到 NVENC、QSV 或 AMF 后提供可选硬件编码，并分别基准测试；
- 增加更多真实手机、录屏、相机和可变帧率视频样本，形成跨设备测试矩阵。
